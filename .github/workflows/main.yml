on: [push]

jobs:
  firstWorkflow:
    uses: Remoliveira/id-analyzer-action3/.github/workflows/main.yaml@000i

  analyzerJob:
    runs-on: ubuntu-latest
    name: A job to say thank you
    steps:
      - name: Analyzer
        id: hello
        uses: Remoliveira/id-analyzer-action3@000i
        with:
          repo: ActionTest
          owner: Remoliveira
          branch: master
      # Use the output from the `hello` step
      - name: Get the output time
        run: echo "The time was ${{ steps.hello.outputs.out }}"
